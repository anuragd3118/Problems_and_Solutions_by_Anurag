#include <iostream>
#include <vector>
#include <string>
#include <set>
#include <unordered_set>
#include <map>
#include <unordered_map>
using namespace std;

void solve(set<vector<int>> &ans, int sum, vector<int> &temp)
{
    if (sum == 0)
    {

    }
    for (int i = 1; i <= 6; i++)
    {
        
    }
    
}
int main()
{
    int n;
    cin >> n;
    set<vector<int>> ans;
    return 0;
}